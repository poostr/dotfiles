# Глобальные инструкции

## Поиск информации в интернете

Когда нужно найти актуальную информацию в интернете, использовать MCP сервер **exa** (инструменты `mcp__exa__*`).

Для авторизации используется переменная окружения `EXA_API_KEY` (уже установлена в окружении).
